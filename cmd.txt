npm run dev    
npm run dev -- --host
npm run build
npx tsc --noEmit

npm uninstall monaco-editor

git status
git commit -m "Initial App Diagnostics web project"
git push -u origin main



git add .
git commit -m "Minimize Firestore reads: First load: fetch the initial log set. Subsequent refreshes: query only logs newer than the latest timestamp already stored locally. This avoids repeatedly reading the entire log collection."
git push